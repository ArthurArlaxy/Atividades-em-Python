let linguagem = prompt('Qual a linguagem de programação que você mais gosta? ');

console.log(linguagem);

let v1 = parseInt(prompt('Digite o primeiro valor: '));
let v2 = parseInt(prompt('Digite o primeiro valor: '));

let resultado1 = v1 + v2;
let resultado2 = v1 - v2;

console.log(`A diferença entre ${v1} + ${v2} é igual a ${resultado1}`);
alert(`A diferença entre ${v1} + ${v2} é igual a ${resultado1}`);
console.log(`A diferença entre ${v1} - ${v2} é igual a ${resultado2}`);
alert(`A diferença entre ${v1} - ${v2} é igual a ${resultado2}`);
